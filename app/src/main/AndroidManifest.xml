<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smileComchase">

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShoppingPage">
        <activity android:name=".registerdblog"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TomatoDetailActivity" />
        <activity android:name=".PotatoDetailActivity" />
        <activity android:name=".MoneyActivity2" />
        <activity android:name=".MoneyActivity" />
        <activity android:name=".VegeListActivity" />
        <activity android:name=".FlowerTopActivity" />
        <activity android:name=".DiaryActivity" />
        <activity android:name=".RealmTestActivity" />
    </application>

</manifest>